# 实物产品视觉出图 — Phase 2 方案层 v1.1 用户验收测试（UAT）

> **状态**：草案待审  
> **日期**：2026-08-11  
> **读者**：产品、运营、测试、演示验收人员（**非研发单元测试**）  
> **功能规格**：[2026-08-11-product-visual-phase2-scheme-ssot-design.md](./2026-08-11-product-visual-phase2-scheme-ssot-design.md)  
> **研发 TDD 用例**：[2026-08-11-product-visual-phase2-scheme-ssot-test-cases.md](./2026-08-11-product-visual-phase2-scheme-ssot-test-cases.md)（pytest/eval，与本 UAT **互补**）

| 字段 | 值 |
|------|-----|
| 文档版本 | v1.1 |
| 验收对象 | `flow_mode: product_visual` + `product_visual_scheme_v2=true` |
| 验收方式 | **真实浏览器** + 真实/预发环境；以 **用户可见行为** 为准 |
| 体验规格 | [2026-08-11-agent-conversation-ux-product-visual-design.md](./2026-08-11-agent-conversation-ux-product-visual-design.md) |

---

## 〇、三份文档如何分工

| 文档 | 回答的问题 | 谁用 |
|------|------------|------|
| **功能规格 v1.1** | 系统应做什么 | 产品 / 架构 / 研发设计 |
| **TDD 用例规格** | 研发如何用自动化证明 | 研发 / CI |
| **本 UAT 文档** | 最终用户/产品如何 **手工验收通过** | 产品 / QA / 演示 |
| **对话体验规格** | Agent 输出呈现、13 类体验问题与验收 | 产品 / 前端 / Runtime |

> **原则：** UAT 不通过 = 功能未交付，与 CI 是否全绿 **独立**（CI 可绿但体验不对，仍以 UAT 为准）。  
> **对话体验 v2：** 见 **§十 UX-PV 清单**；P0 四项（UX-PV-01~04）与 CVS-02-AB 口语路径 **同批 Sign-off**。

---

## 一、验收环境与准备

### 1.1 推荐环境

| 项 | 建议值 |
|----|--------|
| 环境 | 预发或生产（需已部署 v2 能力） |
| 入口 | Web 画布 + 右侧 Agent 侧栏 |
| 测试账号 | 具备积分、可登录（示例：`17279698608` / 预发密码） |
| 技能 | 侧栏选择 **「实物产品视觉出图」** |
| Feature | `product_visual_scheme_v2=true`（新会话默认） |

### 1.2 通用前置（每条 UAT 开始前）

- [ ] 使用 **已持久化的真实画布 session**（URL 为 `cms...`，**非** `demo-{timestamp}` 未落库地址）
- [ ] **新建对话**（避免旧 checkpoint 干扰）
- [ ] 产品图已就绪：侧栏附件 **I1** 或本地上传 / 资产库选取，**role=product** 优先
- [ ] 网络稳定；验收过程中 **勿切换浏览器标签页**（SSE 进行中）

### 1.3 验收记录模板

每条用例记录：

| 字段 | 内容 |
|------|------|
| UAT-ID | 如 UAT-CVS-02-001 |
| 执行人 / 日期 | |
| 环境 URL | |
| 画布 session ID | |
| 结果 | ✅ Pass / ❌ Fail / ⚠️ Blocked |
| 截图/录屏 | 文件名或链接 |
| 备注 | 与预期的差异 |

---

## 二、用户旅程总览（验收检查表）

按时间顺序，用户在一次完整任务中应经历：

| 序号 | 用户阶段 | 用户看到什么 | 用户做什么 | 通过标准（摘要） |
|------|----------|--------------|------------|------------------|
| 1 | 准入 | 识图 QA 提示（若需要） | 选「重拍」或「生成白底图」 | 理由 **可读**；非纯技术 JSON |
| 2 | 方案初稿 | 侧栏 **长文行业方案** | 阅读 | **对话框 prose**，画布 **无** 方案节点 |
| 3 | 宏观选择 | A/B/C **卡片** + 推荐标签 | 勾选 1~2 套；确认 | 单套时 **无** 卡片打断 |
| 4 | SSOT 落盘 | 画布出现 **方案文本节点** | 可点开阅读/编辑 | 内容为 **选定 prose**，非 JSON |
| 5 | 构图拆解 | 侧栏 **shot 列表**；画布构图组（可折叠） | 确认或删减 shot | 类型覆盖 utterance 点名场景 |
| 6 | 拓扑确认 | 拓扑 **卡片列表**（非 mermaid 源码） | 「确认构图，生成预览」→「开始出图」 | 两阶段按钮 **文案不同** |
| 7 | 出图等待 | 进度卡「已完成 a/b · 正在生成：…」 | 等待 | 顶部提示 **勿切标签页** |
| 8 | 定稿交付 | 按 **用户原话** 分组候选图 | 切换候选 / **确认全部定稿** | 结束语为 **交付清单表**，非「成功 N 失败 M」 |

---

## 三、经典场景 UAT（CVS 用户路径）

### UAT-CVS-02 — 大闸蟹包装（P0 必过）

**角色：** 电商运营，中秋礼盒包装需求  
**话术：**

> 这是我们中秋要卖的大闸蟹，帮我设计礼盒和快递运输包装视觉，要保鲜防损。出包装效果图、冷链/缓冲结构示意，再加一张模特手持礼盒的送礼效果图，要有中秋节日氛围。

**附件：** 大闸蟹实拍（资产库「大闸蟹实拍」或等价 product 图）

#### 步骤与预期

| Step | 用户操作 | 预期（用户可见） | UAT-ID |
|------|----------|------------------|--------|
| 1 | 新建画布 → 新建对话 → 选技能 → 选附件 I1 → 发送话术 | Agent 开始执行 | UAT-CVS-02-001 |
| 2 | 若出现 QA 门控 | 文案说明 **识图结论**（如清晰度/是否白底/产品识别）；可选「生成白底图」 | UAT-CVS-02-002 |
| 3 | 等待 Phase 2a | 侧栏出现 **完整包装方案正文**（冷链、礼盒、中秋元素等），**不是** JSON 块 | UAT-CVS-02-003 |
| 4 | 若有多套宏观方案 | 卡片展示 **方案 A/B/C**（或等价标签），**至少一套带「推荐」及理由** | UAT-CVS-02-004 |
| 5 | 勾选 **一套** 方案 → 确认 | **此步骤之前画布无方案节点** | UAT-CVS-02-005 |
| 6 | 确认后看画布 | 出现 **方案文本节点**，正文为所选方向 prose，**人类可读** | UAT-CVS-02-006 |
| 7 | 等待拆解 | 侧栏或门控展示 **≥3 个 shot**：包装主视觉、结构示意、模特手持（名称可中文） | UAT-CVS-02-007 |
| 8 | 确认 shot / 拓扑 → 确认出图 | 白底/四视图 **在 SSOT 之后** 再生成（lazy）；节点陆续出图 | UAT-CVS-02-008 |
| 9 | 定稿门控 | 按 **场景/shot** 切换候选；「确认全部定稿」可用 | UAT-CVS-02-009 |
| 10 | 对照 utterance | 最终交付覆盖：**包装效果图 + 结构示意 + 模特送礼**（允许部分出图失败但需明确提示） | UAT-CVS-02-010 |

**❌ 常见 Fail 现象（记录即 Fail）：**

- 侧栏只出现 JSON / `visual_intent` 字段，无行业长文  
- 未确认方案前画布已有方案 JSON 节点  
- Plan 只拆 **1 类** 图（仅白底主图），无包装/模特  
- SSE 立即断开（多为 `demo-*` 画布未落库）  
- 定稿门控从未出现  

---

### UAT-CVS-02-AB — 巨峰葡萄 A+B 双选（P1，口语 + 卡片）

**推荐话术（不在话术里写风格名）：**

> 这是我刚拍的巨峰葡萄，想做成礼盒在网上卖。帮我看看礼盒怎么设计好看，快递怎么包不容易压坏，再要一张有人拿着礼盒送人的图。

**附件：** 巨峰葡萄实拍（资产库「巨峰葡萄实拍」或等价 product 图）

| Step | 用户操作 | 预期（用户可见） | UAT-ID |
|------|----------|------------------|--------|
| 1 | 新建对话 + I1 + 口语话术 | Agent 开始；**不出现**「红金/牛皮纸」除非用户说了 | UAT-CVS-02AB-001 |
| 2 | 识图 QA（若出现） | 标题 **非**「格式异常」；选项含「就用这张图，继续」 | UAT-CVS-02AB-002 |
| 3 | 宏观卡片 | 可 **多选 A+B**；底栏说明 **预计张数 / 分配方式** | UAT-CVS-02AB-003 |
| 4 | SSOT | 节点含 **「## 方案 A」「## 方案 B」** 两节 | UAT-CVS-02AB-004 |
| 5 | 定稿 | 分组标题为用户语言（礼盒/防压/送人）；能说出 **哪张是哪套** | UAT-CVS-02AB-005 |

**❌ 常见 Fail：**

- 话术未提风格，方案 prose 却写「红金风/牛皮纸风」  
- 双选后用户以为「每类各 2 张」但 UI 未说明混排策略  
- 侧栏可见 `__macro_scheme_decision__` JSON  

---

### UAT-CVS-02-AB-legacy — 大闸蟹 A+B（话术内指定风格，兼容）

**话术追加：** 「红金礼盒和极简牛皮两套方案我都要出图。」

| Step | 预期 |
|------|------|
| 1 | 宏观卡片可 **多选 2 套**（第 3 套应被阻止或提示） |
| 2 | SSOT 节点内分 **「方案 A」「方案 B」** 两节 |
| 3 | shot / 定稿覆盖 A、B 宏观方案 |
| 4 | 定稿后交付张数与 UI 预期说明 **一致** |

---

### UAT-CVS-01 — 保温杯电商全套（P0）

**话术：**

> 这是我们要上架的保温杯，帮我出一套电商推广图：天猫主图、详情图、模特展示图、模特手持使用的效果图、卖点图、推广海报。风格简约高级，强调316不锈钢和12小时保温。

**附件：** 保温杯 product 实拍

| 检查项 | 预期 |
|--------|------|
| 方案 prose | 含 listing / 卖点 / 风格描述 |
| **不应出现** | 用户未提的 **包装类** shot |
| shot 覆盖 | 主图、详情、模特、手持、卖点、海报 **六类**（或 utterance 等价拆分） |
| 定稿 | 每类场景至少 1 张可定稿 |

---

### UAT-CVS-03 — 客厅空间（P0）

**话术：**

> 这是待装修客厅，出有人在里面的现代简约空间效果图、材质软装搭配板，沙发置入效果，最好有一位女性在客厅使用沙发的真实生活感。暖白原木色调。

**附件：** 空间/沙发 product 图

| 检查项 | 预期 |
|--------|------|
| QA | **无白底** 仍可继续（室内放宽） |
| shot | 空间效果图、材质板、产品置入/人物场景 |
| **不应出现** | 包装类 shot |
| 人感 | 至少 1 张含人物生活场景 |

---

## 四、分阶段 UAT（按规格 Phase）

### Phase 1 — 识图准入（UAT-P1）

| UAT-ID | 场景 | 操作 | Pass 标准 |
|--------|------|------|-----------|
| UAT-P1-001 | 清晰 product 图 | 正常上传 + 包装需求 | QA **通过或静默**进入方案；侧栏可见识图相关说明 |
| UAT-P1-002 | 模糊/杂底图 | 故意上传非白底模糊图 | 出现 QA 门控；选项含 **重拍** / **生成白底图** |
| UAT-P1-003 | 选「生成白底图」 | 点击后继续 | 后续仍按 **原 utterance 意图** 出包装/电商，**不**变成「只出白底」 |
| UAT-P1-004 | 选「我重新拍摄上传」 | 点击后 | 方案/画布 SSOT **清空**；可重新上传再走 |
| UAT-P1-005 | lazy seed | CVS-02 全程观察 | **先** 有 SSOT 方案节点，**后** 出现白底/四视图出图 |

---

### Phase 2 — 对话框方案 + 宏观选择 + SSOT（UAT-P2）

| UAT-ID | 场景 | Pass 标准 |
|--------|------|-----------|
| UAT-P2-001 | 方案初稿 | 侧栏 **长文 prose** ≥ 约 200 字；读起来像 **行业方案**，不是 prompt 列表 |
| UAT-P2-002 | 草案不落画布 | 从发话到宏观确认前，画布 **无** 方案文本节点 |
| UAT-P2-003 | 单套方案 | 仅 1 套时 **不出现** 选择卡片，直接进入 SSOT |
| UAT-P2-004 | 多套方案 | ≥2 套卡片；**推荐** 标醒目 + **理由** 一句话可读 |
| UAT-P2-005 | SSOT | 确认后画布 **1 个方案节点**；内容与所选一致 |
| UAT-P2-006 | 编辑 SSOT | 用户在画布改方案文字 → 提供「重新拆解」或等价提示 | 
| UAT-P2-007 | 修订方案 | 对话「方案改成更简约」≤3 次 | 重新出 prose + 卡片；旧 SSOT 被替换 |
| UAT-P2-008 | 修订超限 | 第 4 次仍要改 | 提示超限；按 **推荐方案** 继续 |

---

### Phase 3 — 拆解 / 确认 / 出图（UAT-P3）

| UAT-ID | 场景 | Pass 标准 |
|--------|------|-----------|
| UAT-P3-001 | shot 清单 | 确认出图前可见 **shot 表**（场景名、类型、所属宏观方案） |
| UAT-P3-002 | 删减 shot | 对话「不要结构示意」 | 更新清单后 **不再** 出该图 |
| UAT-P3-003 | 拓扑门控 | 展示拓扑；点「确认出图」后才开始 | 
| UAT-P3-004 | 并行出图 | 多 shot 同时「出图中」 | 
| UAT-P3-005 | 部分失败 | 模拟上游超时 | 弹窗「部分出图未完成」；可选 **确认已完成** |
| UAT-P3-006 | 与手工一致 | 同图同话术：Agent SSOT prose **体裁** 与手工点「生成方案」相近 | 主观 ✓/✗ + 备注 |

---

### Phase 4 — 定稿交付（UAT-P4）

| UAT-ID | 场景 | Pass 标准 |
|--------|------|-----------|
| UAT-P4-001 | 分组 | 定稿 UI 按 **场景/shot** 分组（可 rollup 类型标题） |
| UAT-P4-002 | 切换候选 | 同 shot 多候选切换 | **不** 触发重新生成 |
| UAT-P4-003 | 微调重绘 | 点某 shot「微调重绘」 | **仅该 shot** 重出 |
| UAT-P4-004 | 确认定稿 | 「确认全部定稿」 | 任务结束；画布保留定稿图 |
| UAT-P4-005 | 每场景 1 张 | 交付后 | 每个 shot **exactly 1** 张定稿 |

---

## 五、负向与边界 UAT（用户视角）

| UAT-ID | 场景 | 操作 | Pass 标准 |
|--------|------|------|-----------|
| UAT-NEG-001 | 无附件 | 只发包装话术 | 明确提示需产品图；**不** 静默胡出 |
| UAT-NEG-002 | 资产库无 role | 资产库选图未标 product | 仍应识图或提示绑定 product；**不** 只出 1 类白底 |
| UAT-NEG-003 | demo 画布 | 新建未落库即发消息 | **不应** 出现（应修复为禁用或自动 create session） |
| UAT-NEG-004 | 切标签 | 出图中断言切走 | 回来后可继续或明确失败提示 |
| UAT-NEG-005 | 多选第 3 套宏观 | 勾 A+B+C | UI 阻止或提示 **最多 2 套** |
| UAT-NEG-006 | 中止 | 中途「重新拍」 | 方案节点、出图节点、附件 **清空** |

---

## 六、与手工画布对齐验收（UAT-MANUAL）

**目的：** 验证 Agent 路径与 **用户手工** 路径体验一致。

| 步骤 | 手工路径 | Agent 路径 | 对齐点 |
|------|----------|------------|--------|
| 1 | 新建文本节点 + 挂 product 图 | 侧栏附件 I1 | 同一张 product ref |
| 2 | 输入同一句 CVS-02 话术 | 同话术 | |
| 3 | 点节点「生成方案」 | 等 Agent 2a prose | **体裁** 均为行业方案长文 |
| 4 | 用户自己复制方案到节点 | Agent 2c SSOT 节点 | 均为 **可读 prose** |
| 5 | 手工建 shot 文本 + 连线出图 | Agent 自动拆解+编排 | Agent 省步骤，但 **shot 语义** 应覆盖用户会写的构图 |

**UAT-MANUAL-001 Pass：** 产品评审认为 Agent SSOT 与手工生成方案 **同一类文档**（可编辑、有行业细节，非 JSON）。

---

## 七、发布门禁（产品 Sign-off）

以下 **全部 P0** UAT ✅ 方可对外宣称 v1.1 方案层交付：

| 编号 | 用例 |
|------|------|
| G1 | UAT-CVS-02-003 ~ 010（大闸蟹主路径） |
| G2 | UAT-CVS-01 全套 shot 覆盖 |
| G3 | UAT-CVS-03 室内 + 白底放宽 |
| G4 | UAT-P2-001 / 002 / 005（prose + 无草案画布 + SSOT） |
| G5 | UAT-P1-005（lazy seed 顺序） |
| G6 | UAT-P4-001 ~ 004（定稿门控） |
| G7 | UAT-NEG-003（demo 画布）或已知 issue 书面豁免 |

**P1 建议：** UAT-CVS-02-AB（口语路径）、UAT-P2-007、UAT-MANUAL-001  

**对话体验 v2.1（P0 UX，与 v1.1 同批或紧随其后 Sign-off）：**

| 编号 | 用例 |
|------|------|
| G8 | UAT-UX-PV-01 ~ 04 **全部 Pass**（§十一） |

**对话体验 v2.2~v2.3（P1/P2 UX，建议下一迭代 Sign-off）：**

| 编号 | 用例 |
|------|------|
| G9 | UAT-UX-PV-05 ~ 09 Pass |
| G10 | UAT-UX-PV-10 ~ 13 Pass（G10-合并门控需 `LNKPI_PV_MERGED_SHOT_TOPO_GATE=true`） |

---

## 八、UAT-ID ↔ 规格映射

| 规格规则 | 代表 UAT |
|----------|----------|
| R-Dialog-Draft | UAT-P2-001, UAT-CVS-02-003 |
| R-No-Draft-Canvas | UAT-P2-002, UAT-CVS-02-005 |
| R-Canvas-SSOT | UAT-P2-005, UAT-CVS-02-006 |
| R-Macro-Scheme-Select | UAT-P2-003/004, UAT-CVS-02-004 |
| R-Shot-Decompose / Confirm | UAT-P3-001, UAT-CVS-02-007 |
| R-Phase4-Delivery | UAT-P4-*, UAT-CVS-02-009 |
| R-Vision-QA | UAT-P1-001/002, UAT-CVS-02-002 |
| R-Abort-Clean | UAT-P1-004, UAT-NEG-006 |
| R-UX-Presentation | UAT-UX-PV-01 ~ 13（§十） |

研发 AC-ID 详见 TDD 文档 §十二；**UAT 与 AC 双轨都绿** 再发版。

---

## 九、验收执行建议

1. **第一轮（产品）：** 只跑 §三 CVS 三条主路径 + §七 G1~G6  
2. **第二轮（QA）：** 补 §四 Phase 分项 + §五负向  
3. **第三轮（对齐）：** §六 与手工画布对比  
4. 每条 Fail 附：**截图 + session ID + 侧栏最后 3 条消息**  
5. **对话体验：** 第四轮跑 **§十 UX-PV**（可与 CVS-02-AB 口语路径合并执行）

---

## 十、对话体验 UAT（UX-PV-01 ~ 13）

> 来源：[对话体验规格 v1.0](./2026-08-11-agent-conversation-ux-product-visual-design.md) §四  
> **验收方式：** 浏览器侧栏 **用户可见** 行为；禁止以 thread 内 machine payload 代替 UI 验收。

### 10.1 P0 — 必过（v2.1 Sign-off）

| UAT-ID | 问题 | 操作 / 场景 | Pass 标准（用户可见） |
|--------|------|-------------|----------------------|
| UAT-UX-PV-01 | 识图 QA 文案 | 上传果园/非白底 product 图，触发 QA | 主标题 **非**「格式异常/JSON」；选项含 **「就用这张图，继续」**；检查项为人话（清晰度/白底 ✓✗） |
| UAT-UX-PV-02 | 双「确认出图」 | CVS-02 走到 shot 确认 → 拓扑门控 | 第一次主按钮 **「确认构图，生成预览」**；第二次 **「开始出图（约 X 分钟）」**；步骤条可见当前阶段 |
| UAT-UX-PV-03 | A+B 预期 | CVS-02-AB 口语路径，卡片双选 A+B | 宏观卡片底栏说明 **预计张数 + 分配策略**（混排 vs 全量）；定稿时能对应 **方案 A/B** |
| UAT-UX-PV-04 | 话术污染 | 同 thread 先错话术后口语版 + 卡片双选 | 方案 prose / 需求摘要 **不含** 用户未说的风格名；可选 callout「已按最新描述执行」 |

### 10.2 P1 — 强烈建议（v2.2 Sign-off）

| UAT-ID | 问题 | 操作 / 场景 | Pass 标准 |
|--------|------|-------------|-----------|
| UAT-UX-PV-05 | 方案不可扫读 | 等待方案初稿 | 默认见 **「我理解您的需求」+「设计方向摘要」**；可展开全文；宏观卡片 summary ≤80 字 + 推荐理由 |
| UAT-UX-PV-06 | Mermaid 不友好 | 拓扑门控 | 默认 **卡片列表**；无 `flowchart LR` 源码；可折叠「查看技术拓扑」 |
| UAT-UX-PV-07 | 出图进度 | 确认出图后等待 | 进度 **a/b** + 当前节点中文名；顶部 **勿切标签** 提示 |
| UAT-UX-PV-08 | 定稿缺用户语言 | 定稿门控 | 分组标题 = 用户原话（礼盒/防压/送人）；subtitle 含 `[方案A]` 等；底栏 **「确认后将交付 N 张」** |
| UAT-UX-PV-09 | 结束语像日志 | 确认全部定稿后 | **交付清单表** + 基础资产单独小节；**非**唯一结束语「成功 5 失败 0」 |

### 10.3 P2 — 体验 polish（v2.3 Sign-off）

| UAT-ID | 问题 | 操作 / 场景 | Pass 标准 |
|--------|------|-------------|-----------|
| UAT-UX-PV-10 | 门控过多 | 开启 `LNKPI_PV_MERGED_SHOT_TOPO_GATE=true` 后跑 CVS-02 | 硬停 **≤3 次**（含 QA/宏观/定稿）；合并门控按钮「确认构图并开始出图」 |
| UAT-UX-PV-11 | 侧栏分工 | 任意门控 | 上摘要 / 中决策 / 下主按钮 **一屏可决策**；assistant 气泡 **无** `__macro_scheme_decision__` |
| UAT-UX-PV-12 | 重拍 dead-end | QA 选「重新拍摄」→ 上传新图 → 继续 | **无需重打话术**；有 callout + 上传高亮 + 「继续」chip + toast |
| UAT-UX-PV-13 | 缺引导 | 新建对话选 product-visual 技能 | 3 条 **可点击示例话术**；宏观上方「风格在卡片选」；附件区 hint |

### 10.4 UX-PV 常见 Fail（记录即 Fail）

- 侧栏主标题出现「识图模型返回格式异常」  
- 两阶段门控按钮都叫「确认出图」  
- 双选 A+B 但用户无法说清「一共定稿几张、哪张哪套」  
- 定稿卡片标题为 `packaging_hero__1` 等内部 ID  
- 流程结束仅「成功 N 失败 M」，无交付清单  

### 10.5 v2.1 闭环补项（2026-08-12）

| 项 | 状态 | 说明 |
|----|------|------|
| 生成钮 streaming 取消 | ✅ 已补 | 侧栏 `DockGenerateButton` 点击 → abort SSE + toast；**后端 run 仍可能继续**（见 [中断改意图规格](./2026-08-12-agent-mid-run-interrupt-design.md)） |
| `shot_table` 表格 UI | ✅ 已补 | `AgentShotTable.vue` + envelope `body.shots[]` |
| 画布产出默认 5 项折叠 | ✅ 已有 | `COLLAPSE_THRESHOLD=5`（`agentCanvasOutputs.ts`） |
| 合并门控生产开关 | ✅ 脚本默认开 | `enable-agent-runtime.sh` 写入 `LNKPI_PV_MERGED_SHOT_TOPO_GATE=true` |
| §十 正式 Sign-off | ⏳ #227 部署后复测 | 见 §10.6 |
| 出图中途后端 cancel | ⏳ 规格化 | 见 [2026-08-12-agent-mid-run-interrupt-design.md](./2026-08-12-agent-mid-run-interrupt-design.md) |

### 10.6 生产浏览器 UAT 记录（2026-08-12，#225 部署后）

**环境：** `http://119.29.173.89:8888` · 画布 `cmsome3or004as601j0tkzh1u` · CVS-02-AB 口语 · A+B 双选

| UAT-ID | 结果 | 备注 |
|--------|------|------|
| UX-PV-01 | ⚠️ | 友好按钮 +「自动识图暂时不可用」；历史气泡仍见内部 reason（#226 前端过滤） |
| UX-PV-02 | ❌→修 | 两阶段均「确认出图」；根因 `presentation` 未入 checkpoint（#226） |
| UX-PV-03 | ⚠️ | A+B 可双选；footer 预计张数/分配策略未稳定可见 |
| UX-PV-04~05 | ✅ | 口语未污染；需求摘要结构正确 |
| UX-PV-06 | ⚠️ | topo 门控出现；未目视卡片 vs mermaid（presentation 丢失时走 legacy 钮） |
| UX-PV-07 | ⚠️ | 节点级进度 + 任务列表；非严格 a/b 计数 |
| UX-PV-08 | ⚠️ | 「确认全部定稿」可用；分组标题多为 shot label /「默认」，缺用户原话 + `[方案A/B]` |
| UX-PV-09 | ❌→修 | 结束仅「✅ 您的packaging_design视觉稿已就绪」；交付清单表未渲染（#226） |
| UX-PV-11 | ❌→修 | 用户气泡可见 `__macro_scheme_decision__` / `__delivery_decision__`（#226 隐藏） |
| §10.5 取消钮 | ✅ | streaming →「取消生成」 |
| §10.5 画布折叠 | ✅ | 「展开全部 5/23 项」 |

---

## 十一、文档维护

| 变更 | 更新 |
|------|------|
| 规格 v1.1 规则变更 | 先改规格 → 同步本 UAT §八映射 |
| 新增 CVS 场景 | §三 增节 + §七 门禁 |
| 对话体验变更 | 先改 [UX 规格](./2026-08-11-agent-conversation-ux-product-visual-design.md) → 同步 **§十** + §七 G8~G10 |
| 仅研发内部断言 | 只改 TDD 文档，**不必**改本 UAT |

---

## 十二、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-12 | v1.3 | §10.6 生产 UAT 记录 + #226 修复项追踪 |
| 2026-08-12 | v1.2 | §10.5 闭环补项追踪；链至中断改意图规格 |
| 2026-08-11 | v1.1 | 新增 §十 UX-PV-01~13；CVS-02-AB 口语路径；§七 G8~G10 |
| 2026-08-11 | v1.0 | 初稿：Phase 2 方案层功能 UAT |
