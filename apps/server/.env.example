# Shared with Python agent-runtime for Nest internal canvas tools
# Must match Runtime LNKPI_NEST_SERVICE_TOKEN
AGENT_RUNTIME_SERVICE_TOKEN="dev-token"
# When set and GET {url}/health is ok, chat streams via LangGraph runtime; else CanvasAgent
AGENT_RUNTIME_URL="http://127.0.0.1:8000"
